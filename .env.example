# MusicCopilot 运行配置模板：复制为 .env 后按需修改（.env 已被 git 忽略）。
# 真实环境变量优先于本文件 —— Docker 部署用 -e 或 compose env_file 传入同名变量即可。

# SQ Music 后端地址。开发期浏览器直连该地址（需后端开启 CORS）；
# 留空 "" 表示同源，/api 走 Vite 代理转发（转发目标见 MC_DEV_PROXY_TARGET）——
# 通过 https 域名访问时必须用同源模式（https 页面直连 http 接口会被浏览器拦为混合内容）。
# 注意：Docker 容器内该变量必填（nginx 把 /api 反代到该地址），留空时 compose 会自动
# 回退到 MC_DEV_PROXY_TARGET；且容器内 localhost 指向容器自身 —— 后端与 Docker
# 同机时请用 http://host.docker.internal:8096（compose 已配好 host-gateway 映射）
# 或宿主机局域网 IP。
MC_API_BASE_URL=http://192.168.31.170:8096

# 开发代理转发目标（仅 npm run dev / preview 生效），默认取 MC_API_BASE_URL。
# MC_API_BASE_URL 留空（同源模式）时用该项指定真实后端；容器内也作为回退反代目标。
#MC_DEV_PROXY_TARGET=http://localhost:8096

# 域名 / 反向代理访问 dev、preview 时放行的 Host（逗号分隔多个；Vite 默认仅放行 localhost）。
# 仅影响 Vite 开发服务器，与 Docker 部署无关（nginx 不做 Host 校验）。
#MC_ALLOWED_HOSTS=music-copilot.example.com

# Docker 对外端口（仅 docker-compose.yml 读取，默认 17716；应用本身不使用）
#MC_PORT=17716

# 自动登录账号（token 失效也会用它静默重登；留空则不自动登录）。
# 注意：密码避免包含双引号或反斜杠（会破坏生成的 config.json / JSON 转义）。
MC_USERNAME=admin
MC_PASSWORD=admin

# 是否自动登录（true / false，默认 true）。
MC_AUTO_LOGIN=true
