node_modules
**/node_modules
dist
.git
.zcode

# 本机环境配置不进镜像（运行时由 compose environment / docker run -e 注入）
.env
.env.*
.env.example

# 后端（独立镜像，构建上下文 ./server）、刮削存档（不构建不运行）
server
scraper

# 文档站与本地运行数据（SQLite 库/已下载音乐不入构建缓存）
docs
data
downloads
